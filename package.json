{
  "name": "verne",
  "version": "0.0.1",
  "description": "A WYSIWYG library that does not suck",
  "private": true,
  "devDependencies": {
    "chalk": "^2.3.1",
    "lerna": "2.9.0"
  },
  "scripts": {
    "rollup": "rollup -c -w",
    "lerna": "lerna run --parallel watch",
    "build": "node scripts/build"
  },
  "dependencies": {
    "rollup": "0.56.2",
    "rollup-plugin-typescript": "0.8.1",
    "rollup-plugin-typescript-ts-update": "0.8.2"
  }
}
